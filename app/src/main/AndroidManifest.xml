<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zs.itking.bottomlayout">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BottomLayout">
        <activity android:name=".bottombarlayout.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

<!--        <activity android:name=".bottombarlayout.ViewPagerDemoActivity"/>-->
        <activity android:name=".bottombarlayout.LottieDemoActivity"/>
<!--        <activity android:name=".bottombarlayout.FragmentManagerActivity"/>-->
<!--        <activity android:name=".bottombarlayout.DynamicAddItemActivity"/>-->
    </application>

</manifest>